To install the `dev-cli`, run the following command in your terminal:

```bash

bash -c "$(curl -fsSL -H "Cache-Control: no-cache" https://raw.githubusercontent.com/CrisisTextLine/dev-cli-install/refs/heads/main/install.sh)"

```
